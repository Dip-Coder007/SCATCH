<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Scatch Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-gray-50">
    <nav class="w-full flex justify-between items-center px-5 py-3 bg-white shadow-md">
        <h3 class="text-2xl font-bold text-gray-800">Scatch</h3>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin:true %>
        <% if (loggedin){%>
            <div class="flex gap-8 text-gray-700 text-lg">
            <a href="/shop" class="hover:text-blue-600 transition">Shop</a>
            <a href="/cart" class="hover:text-blue-600 transition">Cart</a>
            <a href="/account" class="hover:text-blue-600 transition">My Account</a>
            <a href="/users/logout" class="text-red-600 transition">Logout</a>
        </div>
        <%}%>

    </nav>
